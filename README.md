# AI Restaurant Decision Agent

Simple LangGraph + LangChain LLM agent.

Flow: **Search → Rank → LLM Decision**.

Set `GROQ_API_KEY` in Streamlit Secrets or as an environment variable. Then run `streamlit run app.py`.
